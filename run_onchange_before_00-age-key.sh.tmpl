#!/bin/sh
if [ ! -f "${HOME}/.config/chezmoi/key.txt" ]; then
  mkdir -p "${HOME}/.config/chezmoi"
  op read "op://Personal/chezmoi-age-key/notesPlain" > "${HOME}/.config/chezmoi/key.txt"
  chmod 600 "${HOME}/.config/chezmoi/key.txt"
fi
